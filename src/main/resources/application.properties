
spring.datasource.url=jdbc:postgresql://localhost:5432/Test
spring.datasource.username=postgres
spring.datasource.password=Wenger123$

# Update database
spring.jpa.hibernate.ddl-auto=update
# Show sql query
spring.jpa.properties.hibernate.show_sql=true

# Security
spring.security.user.name=francis
spring.security.user.password=Wenger123$
spring.security.user.roles=ADMIN

# Enable SQL logging
#logging.level.org.hibernate.SQL=debug

# Turn of spring boot banner
spring.main.banner-mode=off

#server.port=129

# Reduce logging level. Set logging level

# logging.level.root=warn

# Setting Time zone
spring.jpa.properties.hibernate.jdbc.time_zone=UTC+1

spring.jpa.open-in-view=false
